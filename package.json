{
  "name": "whitelist",
  "private": true,
  "scripts": {
    "publish": "yarn workspaces foreach npm publish --tolerate-republish",
    "build": "yarn workspaces foreach build"
  },
  "workspaces": [
    "whitelists/*"
  ]
}
